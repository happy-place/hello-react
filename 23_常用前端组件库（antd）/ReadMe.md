## 23_常用前端组件库（antd）
```aidl
前端组件库
1）Ant-Design 蚂蚁金服基于React开发的前端UI组件，简写为antd。
https://ant.design/components/date-picker-cn/#API
安装依赖 ：
    yarn add 'antd' '@ant-design/icons'

导入包：
import {Button,Menu, Dropdown,DatePicker, Space } from 'antd';
import {ArrowLeftOutlined,LeftCircleFilled,DownOutlined} from '@ant-design/icons';
import 'antd/dist/antd.css' // node_modules/antd 下面

使用写好的组件：
    <Button type="primary">primary 按钮</Button>

查看API查找高阶用法展示：

此外还有专门为vue 开发的 UI 库, antd for vue

总结： 1.选；2.拷；3.查;

2）Element-UI 饿了么为Vue开发的UI组件
    针对React也有对应UI库 Element for React

3) Vant 移动端React开发UI组件


```